# PlatformIO build output
.pio/

# Local VS Code configuration
.vscode/

# Operating system files
.DS_Store
Thumbs.db

# Local secrets
.env
*.key
*.pem

# Local release artifacts
dist/
upload-log.txt
# build artifacts
.pio/
sim/build/
sim/shots/

# TUI sim build
sim/tui/build/

# Local Lua/native verification environments
.venv/
.build/
build/
build-native/

# App-signing: compiled host tool (rebuild via tools/app_signing/build.sh).
# The private signing seed is kept OUTSIDE the repo (~/Projects/MeowKit/keys).
tools/app_signing/meowsign
tools/app_signing/*.seed
*.seed.bin
